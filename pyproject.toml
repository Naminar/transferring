[project]
name = "transferring"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=5.0.0.93",
    "pillow>=12.3.0",
    "qrcode>=8.2",
]

[project.scripts]
qr-encode = "transferring.encoder:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
